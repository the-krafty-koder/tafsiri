/*
const assert = require("assert");
const mocha = require("mocha");

const filesaver = require("../downloader/save");

describe("Tests all functions involvong saving of downloaded files",() => {

	beforeEach(()=>{
		let filesaver = new Filesaver("ozark.srt");
	});

	it("tests correct determination of the current directory", () => {
		filesaver.getCurrentDirectory();


	});


})*/